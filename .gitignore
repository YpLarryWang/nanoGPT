.DS_Store
.idea
.ipynb_checkpoints/
.vscode
__pycache__/
*.bin
*.pkl
*.pt
*.pyc
input.txt
env/
venv/
.claude/
.agents/
wandb/
out*/
*.log

# --- BabyLM: keep only the core pipeline code (data/babylm/*.py) in git ---
# tutorial scratch + lesson notes, and any local analysis dumps
tutorials/
analysis/
# derived data artifacts (raw copies, cleaned text, trained tokenizer)
# note: *.bin and *.pkl are already ignored globally above
data/babylm/raw/
data/babylm/clean/
data/babylm/tokenizer/

# 100M-word track data artifacts (mirror data/babylm/; *.bin/*.pkl already ignored globally)
data/babylm_100m/raw/
data/babylm_100m/clean/
data/babylm_100m/tokenizer/
# ablation completion markers (runtime, not code)
results/*.done
